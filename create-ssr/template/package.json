{
  "name": "new-lampjs-app",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start:dev": "bun run --bun lampjs-ssr serve",
    "start:prod": "lampjs-ssr serve prod",
    "build": "tsc && vite build && lampjs-ssr build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "@jacksonotto/lampjs": "latest",
    "@jacksonotto/lampjs-ssr": "latest",
    "vite": "^5.0.4"
  }
}
