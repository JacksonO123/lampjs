{
  "name": "new-lampjs-app",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run --bun lampjs-ssr serve",
    "build": "tsc && vite build && bun run --bun lampjs-ssr build | rollup ssr-dist/main.js -o ssr-dist/main.js",
    "test": "bun run --bun lampjs-ssr build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "@jacksonotto/lampjs": "link:../lib",
    "@jacksonotto/lampjs-ssr": "link:../lib-ssr",
    "rollup": "^4.7.0",
    "vite": "^5.0.4"
  }
}
